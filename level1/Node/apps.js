/*const readLineSync = require('readline-sync');
readLineSync.question("what operation would you like to perform ? ")
const add = "result"
function sum (num1,num2) {
//returns the sum of two numbers

if(add >= add ) {
    return num1 + num2
} 

}
console.log(" The result is " + sum(3,2))

//subtracts two numbers
const sub = "result"
function subtract (num1,num2) {
    if (sub >= sub) {
        return num1 - num2
    } 
}

console.log("The result is " + subtract(3,2));

//multiplies two numbers
const mul = "result"
function multiply (num1, num2){
if(multiply >= multiply){
    return num1 * num2
};
}
console.log("The result is " + multiply(10,8));

//divides two numbers
const div = "result"
function divide (num1,num2) {
if(divide >= divide) {
    return num1 / num2
};
}
console.log(" The result is " + divide(360,90))
*/

const readLineSync = require('readline-sync');
readLineSync.question("what operation would you like to perform ? ")
const add = "result"
function sum (num1,num2) {
//returns the sum of two numbers

if(add >= add ) {
    return num1 + num2
} 

}
console.log(" The result is " + sum(3,2))

//subtracts two numbers
const sub = "result"
function subtract (num1,num2) {
    if (sub >= sub) {
        return num1 - num2
    } 
}

console.log("The result is " + subtract(3,2));

//multiplies two numbers
const mul = "result"
function multiply (num1, num2){
if(multiply >= multiply){
    return num1 * num2
};
}
console.log("The result is " + multiply(10,8));

//divides two numbers
const div = "result"
function divide (num1,num2) {
if(divide >= divide) {
    return num1 / num2
};
}
console.log(" The result is " + divide(360,90))