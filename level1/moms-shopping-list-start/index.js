//form event listener to prevent form from refreshing the web page when adding the list item
const form = document.addItem

form.addEventListener("submit", (event) => {
    event.preventDefault()

    co

})